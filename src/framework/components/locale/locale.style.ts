import styled from 'styled-components';
import { topRightAbsolute } from 'framework/system/theme';

export const Container = styled.div`
  ${topRightAbsolute};
  display: flex;
  justify-content: space-between;
  width: 15vw;
`;
